dependencies {
    runtimeOnly group: 'mysql', name: 'mysql-connector-java', version: '5.1.48'
}
application {
    applicationDefaultJvmArgs = ['-Dprofile=prod']
    applicationName = "${project.name}-prod"
}
distributions {
    main {
        baseName = "${project.name}-prod"
    }
}