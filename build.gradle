subprojects {
    apply plugin : 'java'
    apply from: "${rootDir}/lint.gradle"

    repositories {
        mavenCentral()
    }
}

allprojects {
    group 'edu.bbte.idde.lab-zdim1981'
    version '1.0-SNAPSHOT'
}